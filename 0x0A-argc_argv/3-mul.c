/*
 *
 */

#include <stdio.h>

int main(int argc, char *argv[])
{
	int sum;
	if(argc != 3)
	{
		printf("Error\n");
		return (1);
	}

	a = atoi(argv[i]);
	b = atoi(arg[2]);

	sum = a * b;

	printf("%d\n", sum);
	return (0);
}
