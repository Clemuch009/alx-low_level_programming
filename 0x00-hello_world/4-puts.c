/*
 *Prints "Programming is like building a multilingual puzzle".
 * main() is the entry point of the program. It calls the `puts` function
 * to print the specified message to the standard output (the console).
 */

#include <stdio.h>

int main(void)
{
	puts("Programming is like building a multilingual puzzle");

	return (0);
}
